# Prevent caching of HTML entry point so new deploys always load fresh chunks
/
  Cache-Control: no-cache
/index.html
  Cache-Control: no-cache

# Allow long-term caching of hashed assets (they have unique filenames per build)
/assets/*
  Cache-Control: public, max-age=31536000, immutable
